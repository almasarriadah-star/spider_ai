# Core packages (Phase 1 - works without YOLO)
requests
numpy
opencv-python
pyyaml
pandas
streamlit
folium
streamlit-folium

# PDF report export (with Arabic support)
reportlab
arabic-reshaper
python-bidi==0.4.2
matplotlib

# Optional (Phase 2 - weed detection with YOLO). Heavy; install when model is ready:
# ultralytics
